# StyleLens V6 AI Orchestrator - Environment Variables
# Copy this file to .env and fill in your actual values.

# Gemini API Key (for quality gates and clothing analysis)
GEMINI_API_KEY=your-gemini-api-key-here

# Modal GPU Worker (Tier 4)
# Run `modal setup` or `modal token set` to authenticate.
# Credentials are stored in ~/.modal.toml
MODAL_TOKEN_ID=your-modal-token-id
MODAL_TOKEN_SECRET=your-modal-token-secret
